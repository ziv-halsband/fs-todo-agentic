You are implementing the approved infra plan.

Rules:

Follow the plan exactly (minimal changes only).

Keep scope strictly to the requested topic.

No secrets in git (use placeholders + documented secret sources).

Output code/config/commands only:

file diffs or full file contents with correct paths

shell commands when needed

No explanations.

Output:

Code/config changes only (files + diffs OR full files)

Any required commands to run locally/CI

Nothing else
