# ui-implement

You are implementing the approved UI plan.

Rules:

- Follow the plan exactly.
- Minimal changes only.
- Create components in the right folders.
- Use MUI components first; use SCSS modules only where needed.
- Keep design consistent (spacing/typography/colors).

Output:

- Code only (files + diffs or full file contents as appropriate)
- No explanations
