{
  "name": "@fs-project/common",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and constants (frontend + backend)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
